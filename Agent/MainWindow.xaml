<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:svg="http://sharpvectors.codeplex.com/runtime/"
        xmlns:svgc="http://sharpvectors.codeplex.com/svgc/"
        xmlns:local="clr-namespace:Agent"
        x:Class="Agent.MainWindow"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid Margin="4,4,4,4">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="32" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="32" />
        </Grid.ColumnDefinitions>
        <RichTextBox x:Name="Output" Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" VerticalScrollBarVisibility="Auto" IsReadOnly="True"/>
        <TextBox x:Name="Input" Grid.Row="1" Grid.Column="0" Height="32"  HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" KeyDown="InputOnKeyDown"/>
        <Button Grid.Row="1" Grid.Column="1"  Width="32" Height="32" HorizontalAlignment="Left" VerticalAlignment="Top" Background="{x:Null}" >
            <Image Source="{svgc:SvgImage Source=/icons/Send.svg, AppName=Agent}"/>
            <!--<svgc:SvgViewbox Height="30" Width="30" Source="/icons/Send.svg" />-->
        </Button>
    </Grid>
</Window>